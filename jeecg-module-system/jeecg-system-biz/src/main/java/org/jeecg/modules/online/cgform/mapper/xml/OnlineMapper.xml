<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.online.cgform.mapper.OnlineMapper">

	<!-- 执行select 查询集合 -->
	<select id="selectByCondition" resultType="java.util.LinkedHashMap">
		${sqlStr}
	</select>

	<!-- 执行select 分页查询集合 -->
	<select id="selectPageByCondition" resultType="java.util.LinkedHashMap">
		${sqlStr}
	</select>
</mapper>